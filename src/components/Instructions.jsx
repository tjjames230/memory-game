const Instructions = () => {
	return (
		<p className="text-center">
			Instructions: Get points by clicking on an image but don&apos;t click on
			any more than once!
		</p>
	);
};

export default Instructions;
